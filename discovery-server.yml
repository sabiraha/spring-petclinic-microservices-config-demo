server:
  port: 8761

eureka:
  instance:
    hostname: http://ec2-52-70-141-207.compute-1.amazonaws.com
  # standalone mode
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

